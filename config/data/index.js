const VERSION_KEY = require('../versions').VERSION_KEY;
const PUB = require('../versions').PUB;

module.exports = {
    GAME_CFGS:require('./china') //TODO 开发测试
    // GAME_CFGS:require(`./${VERSION_KEY[PUB]}`)

}